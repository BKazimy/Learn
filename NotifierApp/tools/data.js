class Data {
    constructor(
      id,
      quote, 
      author,
      story,
      source,
    ) {
      this.id = id;
      this.quote = quote;
      this.author = author;
      this.story = story;
      this.source = source;
    }
  }


export const Entries = [
    {
        
    }
]

export default Entries;